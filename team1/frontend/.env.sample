# MEDviz frontend configuration. Copy to .env and adjust as needed.
# WARNING: Create React App bakes every REACT_APP_* var into the public JS
# bundle at build time. Never put secrets (API keys, passwords) here.

REACT_APP_API_BASE=http://localhost:5000   # base URL of the backend API
